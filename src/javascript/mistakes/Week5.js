// Using sort with numbers
// Sort the numbers increasingly
const numbers = [10, 5, 20, 1];
numbers.sort();
console.log(numbers);


const numbers = [3, 1, 4, 2];
// Create a new sorted array
const sortedNumbers = [...numbers].sort();
